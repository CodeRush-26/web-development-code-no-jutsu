{
  "name": "fleet-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "description": "Code Rush — Fleet Command backend (in-memory simulator + WebSocket + Groq AI distress + routing)",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@turf/turf": "^7.2.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "groq-sdk": "^0.12.0",
    "socket.io": "^4.8.1",
    "uuid": "^11.0.5"
  },
  "engines": {
    "node": ">=20"
  }
}
